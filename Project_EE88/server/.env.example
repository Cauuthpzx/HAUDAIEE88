# ═══════════════════════════════════════
# Agent Hub — Environment Variables
# ═══════════════════════════════════════
# Copy file này thành .env và điền giá trị thực

# Application
PORT=3001

# JWT Authentication (BẮT BUỘC thay đổi!)
JWT_SECRET=your-secret-key-min-32-chars-change-this
JWT_EXPIRES_IN=24h

# Encryption — Mã hóa ee88_password trong DB
# Để trống → tự sinh key khi khởi động lần đầu
ENCRYPTION_KEY=

# Fanout — Gọi EE88 API song song
FANOUT_CONCURRENCY=5
FANOUT_TIMEOUT=15000

# Cache — Đồng bộ dữ liệu
CACHE_ENABLED=true
CACHE_CRON=5 0 * * *

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=200
AUTH_RATE_LIMIT_MAX=10
BODY_LIMIT=1mb

# Logging
LOG_RETENTION_DAYS=30

# Captcha Solver (Python service)
SOLVER_PORT=5000
SOLVER_URL=http://localhost:5000

# Legacy — Migrate agent từ .env (chỉ dùng lần đầu)
# EE88_BASE_URL=
# EE88_COOKIE=
